apiVersion: v1
kind: Service
metadata:
  name: backend-prisma-service
  namespace: prod
  labels:
    app: prisma
    tier: backend
spec:
  ports:
    - protocol: TCP
      port: 8085
      targetPort: 8081
  selector:
    app: prisma
    tier: backend